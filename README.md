# dv2-public-data-demo

A minimal, runnable Data Vault 2.0 reference implementation on a public Snowflake dataset. Built with **dbt + AutomateDV** on **Snowflake** to demonstrate hubs, links, satellites, hashdiffs, and a business-vault layer — the patterns enterprise data teams actually use, on data anyone can query.

> **Why this repo exists:** Data Vault 2.0 is widely used at insurance, financial, and healthcare companies, but is rarely demonstrated publicly because most production implementations live behind enterprise firewalls. This is the smallest defensible reference, designed to be read and understood in 5 minutes, not a sprawling enterprise simulation.

> **Why Snowflake:** AutomateDV (formerly dbtvault) is built primarily for Snowflake. It's the reference adapter with the most macro coverage. Most production Data Vault deployments at scale run on Snowflake. This repo runs entirely inside the **Snowflake 30-day free trial**. It uses the **CARTO NYC Taxi** dataset from the Snowflake Marketplace (free, zero-copy), joined in staging against a free **NYC Taxi Zones** polygon listing via `ST_CONTAINS` to derive `pickup_location_id`. Reviewers can reproduce it end-to-end without paying.

---

## What it demonstrates

- **Raw vault structure** — hubs, links, and satellites built with the [`automate_dv`](https://automate-dv.com/) dbt package on Snowflake
- **Hash keys + load metadata** — every raw vault model carries `HASH_KEY`, `LOAD_DATETIME`, and `RECORD_SOURCE` columns generated by AutomateDV macros
- **Hashdiff for change detection** — satellites use a hashdiff column to identify when descriptive attributes change, enabling SCD Type 2 history without column-by-column comparison
- **Business vault layer** — derived metrics (e.g., `tip_pct`, `fare_per_mile`) computed once and reused, separating the immutable raw vault from analytical computations
- **Information mart pattern** — one flat, dimensional table that the consumption layer (Power BI, Tableau, Looker) reads from, hiding raw-vault complexity from BI users
- **dbt tests + dbt-docs** — uniqueness, not-null, and referential tests on every hub and link, plus a hosted lineage graph
- **Snowflake-native patterns** — role/database/schema separation for raw vault vs. business vault vs. information mart, virtual warehouse sizing notes, and a Time Travel demo for audit-grade lineage

---

## Architecture

```
Snowflake Marketplace:                Snowflake Marketplace:
  CARTO NYC Taxi                        NYC Taxi Zones
  (trips, lat/long)                     (GEOGRAPHY polygons)
        │                                       │
        └───────────────┬───────────────────────┘
                        │  (zero-copy mounts, no ingestion)
                        ▼
                  staging
                  ├─ v_stg_trips              (1:1 source view)
                  ├─ v_stg_zones              (1:1 source view)
                  └─ v_stg_trips_with_zones   (ST_CONTAINS join →
                                               derives pickup_location_id)
                        │
                ┌───────┼───────┐
                ▼       ▼       ▼
              hubs    links   satellites      ← raw vault (immutable, append-only)
                │       │       │
                └───────┼───────┘
                        ▼
                Business Vault                ← derived metrics
                        │
                        ▼
                information mart              ← what Power BI / Tableau / Looker reads
```

Full diagram with model names, role/database/schema layout, and grain notes: [`docs/architecture.png`](docs/architecture.png).

---

## Stack

- **Warehouse:** Snowflake (Standard edition, runs entirely on the 30-day free trial / $400 credit)
- **Source data:** [CARTO NYC Taxi](https://app.snowflake.com/marketplace/providers/GZT0ZKUCHE3/CARTO) (TLC trip records, 2014–2015 vintage, free, zero-copy mount) **+** a free **NYC Taxi Zones** polygon listing on Snowflake Marketplace, joined in staging via `ST_CONTAINS` to derive `pickup_location_id`. `[Confirm exact listing names + database/schema/table identifiers when mounting]`
- **Note on data vintage:** the CARTO listing covers 2014–2015 trips. The DV2 patterns demonstrated here are date-range-independent — every macro, hash key, hashdiff, and link relationship works identically on 2024 data. The 2014–2015 range is what's free on Marketplace. The patterns are timeless.
- **Transformation:** [dbt-core](https://github.com/dbt-labs/dbt-core) 1.7+ (CLI, runs locally — primary deliverable)
- **Hosted IDE & lineage screenshot:** [dbt Cloud Developer](https://www.getdbt.com/pricing) (free indefinitely, 1 user / 1 project)
- **Data Vault macros:** [`automate_dv`](https://automate-dv.com/) (formerly `dbtvault`) 0.11+, Snowflake adapter
- **Tests:** dbt built-ins + AutomateDV hashdiff-uniqueness check
- **Docs:** `dbt docs generate` → hosted on GitHub Pages
- **CI (stretch):** GitHub Actions running `dbt build` against the Snowflake trial account on PR

---

## Models

### Staging (`models/staging/`)

| Model | Materialization | Purpose |
|---|---|---|
| `v_stg_trips` | View | 1:1 mirror of the CARTO trip records, light renaming to snake_case, type casting. No business logic. |
| `v_stg_zones` | View | 1:1 mirror of the NYC Taxi Zones polygon listing. Exposes `pickup_location_id`, `zone_name`, `borough`, `zone_geom` (`GEOGRAPHY` type). |
| `v_stg_trips_with_zones` | Table | `ST_CONTAINS` join: assigns each trip a `pickup_location_id` by point-in-polygon against `v_stg_zones`. Materialized as a table so the geo-join cost is paid once, not on every downstream query. |
| `stg_trips_hashed` | View | AutomateDV `stage()` view that adds `vendor_pk`, `pickup_zone_pk`, `link_vendor_zone_pk`, `vendor_hashdiff`, `load_datetime`, `record_source`, `vendor_label`. |
| `stg_zones_hashed` | View | AutomateDV `stage()` view that adds `pickup_zone_pk`, `zone_hashdiff`, `load_datetime`, `record_source`. |

### Raw vault (`models/raw_vault/`)

| Model | Type | Grain | Business key |
|---|---|---|---|
| `hub_vendor` | Hub | One row per vendor | `vendor_id` |
| `hub_pickup_zone` | Hub | One row per taxi pickup zone | `pickup_location_id` (derived in `v_stg_trips_with_zones`) |
| `link_vendor_zone` | Link | One row per vendor × zone combination observed in trips | `vendor_id` + `pickup_location_id` |
| `sat_vendor_details` | Satellite | SCD2 history of vendor descriptive attrs (`vendor_label`) | `hub_vendor.HASH_KEY` |
| `sat_zone_details` | Satellite | SCD2 history of zone descriptive attrs (`zone_name`, `borough`) | `hub_pickup_zone.HASH_KEY` |

### Business vault (`models/business_vault/`)

| Model | Purpose |
|---|---|
| `bv_trip_metrics` | Per-trip derived metrics: `tip_pct`, `fare_per_mile`, `is_peak_hour`, `is_long_trip`. Computed once here, reused by every downstream consumer. |

### Information mart (`models/information_mart/`)

| Model | Purpose |
|---|---|
| `im_vendor_zone_daily` | Flat daily fact at vendor × zone grain. Joins the raw vault + business vault into a single wide table that BI tools can query without understanding hubs/links. |

---

## How to run it

### Option A — `dbt-core` CLI (recommended; the repo is the deliverable)

```bash
# 1. Sign up for Snowflake (30-day free trial, no credit card required for standard trial)
#    https://signup.snowflake.com/ — pick AWS or Azure, any region.

# 2. In the Snowflake UI: Marketplace → search "CARTO NYC Taxi" → "Get" (free, zero-copy mount).
#    Then search "NYC Taxi Zones" (polygons) → "Get" — the staging layer joins them via ST_CONTAINS.

# 3. Clone
git clone https://github.com/linsleymichira/dv2-public-data-demo.git
cd dv2-public-data-demo/dv2_public_data_demo

# 4. Set up your dbt profile (Snowflake adapter)
cp profiles.yml.example ~/.dbt/profiles.yml
# Edit ~/.dbt/profiles.yml with your Snowflake account, user, role, warehouse, database, schema.

# 5. Install dbt-snowflake + AutomateDV
pip install dbt-snowflake
dbt deps                # installs automate_dv + dbt_utils from packages.yml

# 6. Build everything
dbt build               # runs models + tests in dependency order

# 7. Browse lineage + docs locally
dbt docs generate
dbt docs serve          # opens http://localhost:8080
```

Total runtime on an `XSMALL` Snowflake warehouse: **under 3 minutes** end-to-end. Burns roughly 5–10 credits of the $400 trial allowance, or ~2% of the trial budget. Plenty of headroom to rerun.

### Option B — dbt Cloud Developer (hosted IDE, free)

The repo also runs on [dbt Cloud Developer](https://www.getdbt.com/pricing) (free, 1 user). Sign in at `cloud.getdbt.com`, link the GitHub repo, point the connection at your Snowflake trial, and `dbt build`. The screenshot in [`docs/dbt-cloud-lineage.png`](docs/dbt-cloud-lineage.png) shows the lineage graph rendered there.

---

## Free-tier playbook (no spend, no employer overlap)

The whole repo is reproducible at zero cost on personal accounts:

| Component | Free path | Watch out for |
|---|---|---|
| Snowflake | 30-day trial, $400 credit, no card required (standard trial) | Trial clock starts at sign-up. Build the dbt project locally with `dbt parse` first; only activate the Snowflake account once you're ready to run `dbt build`. |
| Source data | CARTO NYC Taxi (trips) + NYC Taxi Zones (polygons), both free on Snowflake Marketplace | None — Marketplace mounts are zero-copy, no storage cost. The geo-join in `v_stg_trips_with_zones` is the only computed staging step. |
| dbt | `dbt-core` CLI is free forever; `dbt Cloud Developer` is free indefinitely (1 user / 1 project) | dbt Cloud has a job-runtime cap on the free tier; for this repo, it's a non-issue. |
| GitHub | Public repo, free. GitHub Actions free tier covers CI on this repo's footprint. | None. |
| GitHub Pages (hosted dbt docs) | Free for public repos | None. |

---

## What's interesting (the stuff hiring managers ask about)

1. **Why a hashdiff instead of column-by-column comparison?** — Satellites detect change by hashing all descriptive columns into a single `HASHDIFF` and comparing only that. This makes change detection O(1) per row, regardless of how many descriptive columns the satellite carries, which is critical when satellites are 50+ columns wide.
2. **Why hash keys instead of natural keys for joins?** — Hash keys (MD5 by default in AutomateDV; SHA-256 configurable) give every entity a fixed-width primary key regardless of source-system key shape. Joins stay fast even when business keys are long strings or composite. On Snowflake, fixed-width hash keys also cluster better.
3. **Why is the link table grain "one row per combination observed" rather than "one row per trip"?** — Trip-grain belongs in a transactional satellite or fact, not in the link itself. The link captures the *relationship* between the vendor and the zone; the *transaction* is captured at the trip-level satellite. This keeps the raw vault immutable: a link row, once inserted, is never updated.
4. **Why a separate business vault?** — Raw vault is a faithful, append-only mirror of the source. The moment you start computing `tip_pct = tip/fare`, you've made an analytical decision that may need to change later. Business vault isolates those decisions, so the raw vault stays trustworthy as the audit-grade source.
5. **Where would this break at scale?** — Single-source demos hide multi-source pain. In production, hubs gather business keys from multiple sources, and the `RECORD_SOURCE` column starts earning its keep. This repo doesn't model that — see the Roadmap.
6. **Why Snowflake (and not BigQuery / Databricks / Postgres)?** — AutomateDV is primarily built and tested for Snowflake — it's the reference adapter. Snowflake's separation of compute (warehouses) from storage maps cleanly onto the Data Vault layering: raw vault and business vault can run on differently-sized warehouses, and zero-copy clones make it cheap to spin up dev replicas of the raw vault. The same patterns *do* port to BigQuery/Databricks (AutomateDV supports both), but the macros are best-tested on Snowflake.

---

## Roadmap (not in v1)

- [ ] Multi-source hub demo (load `hub_vendor` from two synthetic source systems with different key conventions)
- [ ] Multi-active satellite (multiple active rows per parent hub at the same time, e.g., a vendor with multiple active phone numbers)
- [ ] Effectivity satellite (track the *time* a relationship was valid, not just the change in attributes)
- [ ] Reference data + record-tracking satellite patterns
- [ ] Snowflake Time Travel demo: query the raw vault "as of" a prior load timestamp to prove audit-grade history
- [ ] Zero-copy clone demo: stand up a `DEV_RAW_VAULT` clone of `RAW_VAULT` to demonstrate cheap dev/QA isolation
- [ ] CI: dbt build on PR via GitHub Actions against the Snowflake trial account
- [ ] dbt Fusion runtime: migrate package vendoring to use the Fusion-compatible patches drafted on `feat/migrate-to-fusion-autofix`

---

## Note on scope

A common Data Vault demo description reads "1 hub, 2 satellites, 1 link," but a link, by definition, connects 2+ hubs, so the smallest coherent DV2 footprint is **2 hubs + 1 link + 2 satellites + 1 business-vault model**. That's what this repo ships. The intent is the *smallest defensible reference*, not the smallest possible file count.

---

## License

MIT — see [`LICENSE`](LICENSE).

## Author

Associate People BI Analyst @ JD North America (JDNA). I run production GCP pipelines (Airflow + dbt + BigQuery + Data Vault 2.0) for People + Finance teams across a 7-brand retail portfolio. This repo is the public Snowflake reference for the patterns I use at work.

📧 linsleymichira@outlook.com
💼 [LinkedIn](https://linkedin.com/in/linsley-michira)
🔗 [linsleymichira.com](https://linsleymichira.com) `[Confirm site is live before publishing]`
